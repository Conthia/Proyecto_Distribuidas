spring:
  application:
    name: pago
  profiles:
    active: dev
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://localhost:7072}"
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
